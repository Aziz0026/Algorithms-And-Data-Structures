#include <iostream>

using namespace std;

int main() {
  int n;

  for (int i = 0; n > i; i++) {
    bool isPossible;
    int x, y, z;
    cin >> x >> y >> z;
    if (x + y)
      == z ? isPossible = true : isPossible = false;
  }
}
